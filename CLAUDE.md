# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

한국어 발음 게임(잰말놀이) 모바일 웹 앱. React 19 + TypeScript + Vite.

## 참조 문서

작업 전 아래 문서를 먼저 확인하세요.

- [docs/architecture.md](docs/architecture.md) — 명령어, 상태 관리, 라우팅, 게임 로직, 디렉토리 구조, 스타일링
- [docs/conventions.md](docs/conventions.md) — 컴포넌트/네이밍/import 순서/Zustand/Tailwind/훅 작성 규칙

## 작업 원칙

- 해결책을 설계하기 전에, 이미 자리 잡은 제품들이 같은 문제를 어떻게 푸는지 먼저 살펴보세요. 접근 방식을 처음부터 발명하지 말고 검증된 패턴과 관례를 채택하세요.
- 하위 호환을 유지하지 마세요. 호환 레이어·폴백·마이그레이션을 덧붙이는 대신 쓰이지 않는 경로를 삭제하세요.
- 현재 요구사항을 완전히 충족하는 가장 단순한 구현을 선택하세요. 추측에 근거한 추상화, 설정값, 간접 계층을 만들지 마세요.
- 시스템은 레이어로 키우세요. 엔드투엔드로 동작하는 최소 버전에서 시작하고, 이미 동작하는 결과물 위에 기능을 하나씩 얹으세요. 동작하는 코드를 미완성 복잡도와 맞바꾸지 마세요.
- 컴포넌트는 모듈로 분리하고 관심사를 명확히 나누세요.
- 검증되고 유지보수되는 라이브러리가 전체 복잡도를 낮추거나 안정성을 높인다면 그것을 쓰세요. 흔한 기능을 명확한 이유 없이 재구현하지 마세요.
- 직접 구현하거나 패키지를 추가하기 전에 이미 설치된 의존성부터 확인하세요. 문서와 타입을 확인하지 않은 채 "이 라이브러리엔 그 기능이 없다"고 단정하지 마세요.
- 아키텍처 결정은 장기 관점으로 하세요. 지금만 넘기고 나중에 교체할 임시방편을 받아들이지 마세요.

## 프로젝트 예외

### 건드리면 안 되는 경로

- `src/components/ui/` — shadcn/ui CLI가 생성·관리하는 파일. 직접 수정 대신 `src/components/common/`에서 래핑하세요.
- `src/assets/lotties/`, `src/assets/icons/`, `src/assets/images/` — 디자인 산출물. 코드에서 참조만 하고 파일 내용을 편집하지 마세요.
- `pnpm-lock.yaml` — 수동 편집 금지. 패키지 변경은 `pnpm` 명령으로만.

### 작업 완료 전 반드시 실행할 검증 명령

```bash
pnpm lint && pnpm build
```

`pnpm build`는 `tsc` 타입 체크를 포함합니다. 둘 다 통과해야 작업 완료로 봅니다.

### 규제·보안상 반드시 지켜야 할 사항

- 서버·백엔드가 없는 정적 클라이언트 앱입니다. API 키·시크릿을 코드나 `.env`에 두지 마세요.
- 플레이어 닉네임 등 사용자 입력은 로컬(zustand/localStorage)에만 저장하고 외부로 전송하지 마세요.
